{% apply spaceless %}
version: '{{ docker.version }}'
services:
{% for key, service in services %}
{% include service %}
{% endfor %}
networks:
    {{ docker.network }}:
{% if nfs %}volumes:
    ds_nfsmount:
        external: true
{% endif %}
{% endapply %}