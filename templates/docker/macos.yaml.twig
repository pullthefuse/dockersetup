version: '{{ docker.version }}'
volumes:
    ds_nfsmount:
        name: ds_nfsmount
        driver: local
        driver_opts:
            type: nfs
            o: addr=host.docker.internal,rw,nolock,hard,nointr,nfsvers=3
            device: ':${NFS_CODE_DIRECTORY}'